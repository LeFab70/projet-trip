import 'package:flutter/material.dart';

class TripActivity extends StatelessWidget {
  const TripActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My trip'));
  }
}
